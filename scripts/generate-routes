#!/usr/bin/env bash
# Generate @method PHPDoc on ApiRoute enum from #[HasRoute] cases
set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/_env.sh"

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/generate-routes.php
