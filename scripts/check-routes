#!/usr/bin/env bash
# Verify @method PHPDoc on ApiRoute matches #[HasRoute] cases
set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/_env.sh"

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/check-routes.php
