#!/usr/bin/env bash
# Verify @link annotations exist on model classes and ApiRoute cases
set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/_env.sh"

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/check-links.php
