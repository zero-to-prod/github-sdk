#!/usr/bin/env bash
# Run Rector in dry-run mode (no changes)
set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/_env.sh"

version="${PHP_VERSION}"

docker compose run --rm "php${version}" vendor/bin/rector --dry-run "$@"
