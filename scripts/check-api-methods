#!/usr/bin/env bash
# Verify @method PHPDoc on GitHubSdk matches #[AdminApi] attributes
set -euo pipefail

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/check-api-methods.php
