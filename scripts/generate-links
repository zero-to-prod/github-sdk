#!/usr/bin/env bash
# Generate @link annotations on model classes and ApiRoute cases
set -euo pipefail

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/generate-links.php
