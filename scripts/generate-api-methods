#!/usr/bin/env bash
# Generate @method PHPDoc on SdkApi from #[AdminApi] attributes
set -euo pipefail

version="${PHP_VERSION}"

docker compose run --rm "php${version}" php scripts/generate-api-methods.php
